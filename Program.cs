// bool exibirMensagem = true;
int numExibições = 0;
int maxExibições = 273;

while (numExibições < maxExibições)
{
     numExibições = numExibições +1;
     Console.WriteLine($"Hello, World! #{numExibições} vez de um total de {maxExibições}");

     // if (numExibições >- maxExibições)
     // {
     //    exibirMensagem - false;
     // }
     
     // exibirMensagem - numExibições < maxExibições;

}